<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.leng.oldass.dao.MenuRoleDao">

    <resultMap type="com.leng.oldass.entity.MenuRole" id="MenuRoleMap">
        <id property="id" column="id" jdbcType="INTEGER"/>
        <result property="menuId" column="menuId" jdbcType="INTEGER"/>
        <result property="roleId" column="roleId" jdbcType="INTEGER"/>
    </resultMap>

    <delete id="deleteByRoleId">
        delete from menu_role where roleId = #{menuId}
    </delete>
    
    <insert id="insertRecord">
        insert into menu_role (menuId, roleId) values
        <foreach collection="menuId" separator="," item="menuId">
            (#{menuId}, #{roleId}))
        </foreach>
    </insert>
    <!--查询单个-->
    <select id="queryById" resultMap="MenuRoleMap">
        select
          id, menuId, roleId
        from menu_role
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="MenuRoleMap">
        select
          id, menuId, roleId
        from menu_role
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="menuId != null">
                and menuId = #{menuId}
            </if>
            <if test="roleId != null">
                and roleId = #{roleId}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from menu_role
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="menuId != null">
                and menuId = #{menuId}
            </if>
            <if test="roleId != null">
                and roleId = #{roleId}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into menu_role(menuId, roleId)
        values (#{menuId}, #{roleId})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into menu_role(menuId, roleId)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.menuId}, #{entity.roleId})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into menu_role(menuId, roleId)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.menuId}, #{entity.roleId})
        </foreach>
        on duplicate key update
        menuId = values(menuId),
        roleId = values(roleId)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update menu_role
        <set>
            <if test="menuId != null">
                menuId = #{menuId},
            </if>
            <if test="roleId != null">
                roleId = #{roleId},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from menu_role where id = #{id}
    </delete>

</mapper>

